简单工厂模式：
工厂就是用一个单独的类根据用户的需求来实例化要创建的对象，比如有加减乘除四种操作，每种封装成一个
类，这样便于日后的扩展，比如加一个新的运算时不需要在编译原来的类，符合封闭-开放原则。我们不希望将
各个类暴露给用户，所以封装成一个工厂类结合用户的需求来创建对象。

Operation类加减乘除这四个类的基类，工厂运用多态的方式来实例化对象，暴露给用户的只是Operation这个类
以及OperationFactory
