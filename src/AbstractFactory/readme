抽象工厂模式：
工厂模式分为两种：简单工厂模式和抽象工厂模式，简单工厂模式就是一个工厂类，switch case的方式创建
对象，缺点是违反封闭-开放原则，优点是不需要客户端选择创建哪个类。抽象工厂模式弥补了简单工厂模式的
缺点，通过反射+配置文件的方式灵活的创建对象，是Spring框架的实现方式

关键字： 反射 配置文件

抽象工厂模式结构：
Dao层
不同的数据库分别采用了不同的对数据库表的操作方式，如UserDAO有UserDAOSqlServer和UserDAOAccess两种
操作方式，Hibernate在底层肯定也做了这样的实现
关键是如何来创建UserDAO，抽象工厂采用反射的方式，在配置文件中配置了用哪种数据库，通过反射就会调用
哪种数据库的来生成相应的UserDAO

应用场景：
Spring