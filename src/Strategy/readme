策略模式：
策略模式是为了解决策略的切换和扩展，使策略的变化独立于使用策略的客户

Strategy类是一个抽象的策略类，A,B,C是三个策略的实现类。Context是用户的上下文，context对象内引入
具体的策略对象。通过context调用策略的方法。这样当新的策略类加入时，只需要添加一个策略的实现类，
在客户端的context里引入这个策略即可，不需要改动其他的策略类。

策略模式与工厂模式的区别：
策略模式关注于行为的封装
工厂模式关注于对象的创建
比如我有50元钱，想买本书，策略模式会让你选择在淘宝，当当，亚马逊上买，而工厂模式就是告诉系统50元
的书，它不关心是从哪里买来的。策略模式是一个白盒，工厂模式是个黑盒

应用：
优惠系统
工资计算系统



